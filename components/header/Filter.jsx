"use client";

import { Suspense } from "react";
import FilterFunction from "./filterFunction";
const Filter = () => {
  return (
    <Suspense>
      <FilterFunction />
    </Suspense>
  );
};

export default Filter;
